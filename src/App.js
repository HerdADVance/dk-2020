import React from "react";
import ReactDOM from "react-dom";
import { Router } from "@reach/router";
import { Link } from "@reach/router";

const App = () => {
  return (
    <h1>DK Lineup Builder</h1>
  );
};

ReactDOM.render(<App />, document.getElementById("root"));